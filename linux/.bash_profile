# This file is executed when starting a bash login shell.

# Check if the ~/.bashrc file exists, is readable, and is a regular file. If true, then source the ~/.bashrc file.
[ -r ~/.bashrc ] && [ -f ~/.bashrc ] && source ~/.bashrc
